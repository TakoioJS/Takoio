[build]
  command = "npm install -g pnpm@9 && pnpm install && cd src/server/self-hosted && npx tsc"
  publish = "."

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/takoio"
  status = 200
