{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm build:admin",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/takoio" }
  ],
  "functions": {
    "api/takoio.ts": {
      "memory": 256,
      "maxDuration": 30
    }
  }
}
